import * as constants from './constants';

const initial = () => ({});

const comments = (state = initial(), action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default comments
