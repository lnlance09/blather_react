import * as constants from "../constants"

const initial = () => ({
	loading: true
})

const search = (state = initial(), action) => {
	switch (action.type) {
		default:
			return state
	}
}

export default search
